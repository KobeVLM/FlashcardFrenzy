<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlashcardFrenzy"
        android:usesCleartextTraffic="false">

        <!-- Entry point: checks auth state and routes accordingly -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".auth.RegisterActivity" android:exported="false" />

        <!-- Decks -->
        <activity android:name=".deck.DeckListActivity"    android:exported="false" />
        <activity android:name=".deck.DeckDetailActivity"  android:exported="false" />
        <activity android:name=".deck.CreateEditDeckActivity" android:exported="false" />

        <!-- Flashcards -->
        <activity android:name=".flashcard.CreateEditFlashcardActivity" android:exported="false" />

        <!-- Quiz -->
        <activity android:name=".quiz.QuizActivity"        android:exported="false" />
        <activity android:name=".quiz.QuizHistoryActivity" android:exported="false" />

        <!-- Admin -->
        <activity android:name=".admin.AdminActivity"      android:exported="false" />

    </application>

</manifest>
